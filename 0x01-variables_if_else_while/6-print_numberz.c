#include <stdio.h>

/**
 * main - everything goes from here
 * Return: always success comes here
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
