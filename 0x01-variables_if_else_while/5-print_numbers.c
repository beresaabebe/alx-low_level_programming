#include <stdio.h>

/**
 * main - everything goes from here
 * Return: always return success value
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
