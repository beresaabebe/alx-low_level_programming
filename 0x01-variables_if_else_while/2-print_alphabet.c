#include <stdio.h>

/**
 * main - everything starts to run from here
 * Return: show success 0
 */
int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}

	putchar('\n');
	return (0);
}
