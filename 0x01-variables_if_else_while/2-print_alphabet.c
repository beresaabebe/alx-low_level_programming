#include <stdio.h>

/**
 * main every run starts from here
 * Return: show success values 0
 * 
 */
int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	putchar('\n');
	
	return (0);
}
