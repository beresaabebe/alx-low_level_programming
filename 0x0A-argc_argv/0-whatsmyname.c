#include <stdio.h>
/**
 * main - a function that start programming
 * @argc: argument count
 * @argv: argument vector
 * Return: return some values
 */
int main(int argc, int *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
