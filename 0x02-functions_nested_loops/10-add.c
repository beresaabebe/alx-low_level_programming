#include "main.h"
/**
 * add - a function that add two int and return
 * @a = first number, 
 * @b = second number
 * Return: return values of rseult
 */
int add(int a, int b
{
	int result = a + b;

	return (result);
}
