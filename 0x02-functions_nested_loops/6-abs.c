#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @ab: interger inpt value
 * Return: absolute value of abs
 */
int _abs(int ab)
{
	return (ab * (( ab > 0) - (ab < 0)));
}
