#include "main.h"
/**
 *mul - a function that return multiple of two number
 *@a: first number
 *@b: second number
 *Return: return multiple of two number
 */
int mul(int a, int b)
{
	return (a * b);
}
