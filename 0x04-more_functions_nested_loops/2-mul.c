#include "main.h"
/**
 * mul - a function that return multiple of two number
 * @a: first number
 * @b: second number
 * Return: return multple
 */
int mul(unsigned long a, unsigned long b)
{
  return (a * b);
}
